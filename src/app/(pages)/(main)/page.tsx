import type { NextPage } from 'next';

const Page: NextPage = () => {
  return (
    <section className="h-full w-full">
      <div className="container mx-auto flex h-full w-full items-center justify-center">메인 페이지</div>
    </section>
  );
};

export default Page;
