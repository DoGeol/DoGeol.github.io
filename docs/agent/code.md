# 코드 규칙

App Router와 Server Component를 기본으로 한다. Hook·브라우저 API·상호작용 경계만 Client Component로 둔다. TypeScript strict를 지키고 `any` 대신 `unknown`을 좁혀 쓴다. 파일·폴더는 kebab-case, 내부 import는 `@/`를 사용한다. 문서·주석·화면 문구는 한국어가 기본이다.
